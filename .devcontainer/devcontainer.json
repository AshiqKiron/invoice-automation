{
  "name": "Invoice Automation",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "pip install -r requirements.txt && sudo apt-get update && sudo apt-get install -y tesseract-ocr tesseract-ocr-jpn",
  "forwardPorts": [8080]
}